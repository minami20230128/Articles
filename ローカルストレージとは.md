# ローカルストレージとは
ブラウザが端末のストレージにデータを保存できる仕組み。
ユーザ設定、テーマ、ショッピングカートの中身などを保存するのに向いている。

## どういうときにデータが削除されるのか  
基本的に有効期間はなく、localStorage.removeItem()・localStorage.clear()実行時、ユーザが閲覧データを削除したときに消える。  
PC再起動やブラウザ再起動では消えない。

## セキュリティ面  
XSSに弱く、簡単にJavaScriptなどで読み出せてしまう。  
パスワードなど機密性の高い情報はここでは管理するべきではない。