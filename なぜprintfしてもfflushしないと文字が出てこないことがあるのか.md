# なぜprintfしてもfflushしないと文字が出てこないことがあるのか
## 調べようと思ったきっかけ
Fluent Bitの調査をしているとき、本体コードや自作プラグインにprintfを埋め込んでもfflushを実行しないと文字が表示されず苦戦したことがあったため。  

## 理由  
完全バッファリングをされていたから。  
バッファリングとは、メモリ上にデータを一時的に溜めてからまとめて処理すること。
printfは、出力先がターミナルであれば行バッファリング（1行ずつ表示）され、ファイルやパイプであれば完全バッファリングされることが多い。  
例えば以下のような場合。
```
./a.out > result.txt
```
ターミナルに表示する場合は即時出力を確認したいが、ファイルの場合はファイルを開くときに出力結果が確認できればよい。  
なおかつ都度書き込んでしまうとシステムコールが発生し、パフォーマンス的に問題があるためまとめて書き込む。  

## stdoutとは  
標準出力というのは、OSのファイルディスクリプタ1にプログラムが通常の処理結果や成功データを渡して表示すること（Pythonなど他の言語でも同様）。  
C言語で標準出力を扱うものがstdoutで、正体はFILE構造体のポインタ。  
FILE構造体の実装はいくつか種類があるが、出力内容をバッファリングするメモリ領域を持つ。
例えばUNIX系コンパイラであれば以下のようになっている。  
```
extern struct _iobuf{
	int            _cnt;	// 現在位置からファイルの最後までのバイト数
	unsigned char* _ptr;	// 次に読まれる位置(ファイルポインタ)
	unsigned char* _base;	// バッファーの位置
	int            _bufsiz;	// バッファーサイズ
	short          _flag;	// ファイルアクセスモード
	int            _file;	// ファイルディスクリプタ
} _iob[];
#define FILE struct _iobuf
```
例えばprintf("hello");を実行しても文字列が即座に表示されない場合、
この_ptrのところに文字列がバッファリングされている。  
fflushを実行すると、これがOSのファイルディスクリプタ1に引き渡される。