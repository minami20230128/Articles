# フィールドの移動
あるクラスから別のクラスにフィールドを移動すること。  

## 例 顧客クラスから契約クラスに割引率フィールドを移動する  
```class Customer
constructor(name, discountRate) {
    this._name = name;
    this._discountRate = discountRate;
    this._constract = new CustomerConstract();
}
```

```class CustomerConstract
constructor() {
    this.startDate = startDate;
}
```
1. まず移動元でフィールドへのアクセスをカプセル化する  
   ```class Customer
   _setDiscountRate(aNumber) {this._descountRate = aNumber:}
   ```
2. 移動先にフィールドとアクセサを用意する
3. 移動元のアクセサ（_setDiscountRate）を、移動先のdiscountRateフィールドに値をセットするように変更する    
   移動先クラスのインスタンスの初期化の前の行にアクセサ呼び出しが来てしまっている場合は行を入れ替える